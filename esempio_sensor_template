sensor:
  - platform: template
    sensors:
      sensore_virtuale:
        friendly_name: "porta finestra"
        value_template: >
          {% if is_state('switch.sensore_laser', 'on') %}
            "chiusa"  
          {% else %}
            "aperta"
          {% endif %}
      sensore_virtuale2:
        friendly_name: "porta finestra 2"
        value_template: >
          {% if is_state('switch.sensore_laser2', 'on') %}
            "chiusa"  
          {% else %}
            "aperta"
          {% endif %}
