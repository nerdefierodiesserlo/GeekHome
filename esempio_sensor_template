sensor:
  - platform: template
    sensors:
      sensore_virtuale:
        friendly_name: "porta finestra"
        value_template: >
          {% if is_state('switch.sensore_laser', 'on') %}
            "chiusa"  
          {% else %}
            "aperta"
          {% endif %}
